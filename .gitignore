.env
.venv/
__pycache__/
*.pyc
.DS_Store

# Runtime settings written by the task pane's Settings panel.
config.local.json

# Your own routing table and owner context. Start from routes.example.json.
routes.json

# API key fallback store, used only when the macOS Keychain is unavailable.
.credentials
